#include <iostream>
#include <fmt/core.h>
#include <vector>
#include <algorithm>

//Create a function called sameCharacters(), which will accept two arguments - std::vector<char> and an std::string.
//This function should return true if both containers (the passed vector and the passed string)
// consist of the same characters (with the same number of occurrences per character).

int main() {



    fmt::print("Hello there!");

    return 0;
}

bool sameCharacters(std::vector<char> charVec, std::string susString) {
    auto areSame = false;

    if(charVec.size() == susString.size())
    {
        if()
        {
            areSame = true;
        }


    }





    return areSame;
}